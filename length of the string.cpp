//programme to find length of string
#include<stdio.h>
#include<string.h>
main()
{
	char s1[20];
	printf("enter the string");
	gets(s1);
	printf("the length of the string is%d",strlen(s1));
}
