//programme to find area of a circle
#include<stdio.h>
main()
{
	float radius,area;
	printf("enter the radius of the circle:radius,area");
	scanf("%f",&radius);
	area =pi*radius*radius;
	printf("area of the circle=%2f/n",area);
}
